---
import SubServiceLayout from '../../../layouts/SubServiceLayout.astro';
import { patentSubPages } from '../../../data/subpages/patent';
import { patent } from '../../../data/services/patent';

const data = patentSubPages.find((s) => s.slug === 'complete-specification')!;
---

<SubServiceLayout data={data} parent={patent} />
